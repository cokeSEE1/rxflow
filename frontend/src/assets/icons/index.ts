/**
 * 项目自定义 SVG 图标
 *
 * 所有图标统一由 SvgIcon 组件渲染，通过 icon 属性传入。
 * 添加新图标：扔一个 .svg 文件到本目录，在下面加一行 export。
 *
 * 示例：
 *   import BellIcon from './bell.svg'
 *   <SvgIcon :icon="BellIcon" :size="20" color="#333" />
 */

export { default as BellIcon } from './bell.svg'
